I will optimize the font display for mobile devices by adding a responsive CSS section to `css/style.css`.

## Implementation Plan

### 1. Create Mobile-Specific Styles (`@media (max-width: 768px)`)
I will add a media query block at the end of `css/style.css` to target mobile devices specifically.

### 2. Optimize Font Sizes & Readability
- **Base Text**: Set default font size to `1rem` (approx 16px) for body text, ensuring it falls within the requested 14-16px range.
- **Titles (h1, h2)**: Significantly reduce the size of main titles (e.g., `h1` from `5rem` to `2.5rem`, `h2` to `1.5rem`) to prevent them from overwhelming small screens while maintaining hierarchy.
- **Banner/Blessing Text**: Specifically adjust `#banner-text` and `#newyear-overlay` text to be more compact and sharp.
- **Line Height**: Enforce `line-height: 1.5` for better readability.
- **Rendering Quality**: Apply `-webkit-font-smoothing: antialiased` and optimized `text-shadow` for sharper text rendering on high-DPI screens.

### 3. Adjust Layout Elements
- **Buttons**: Resize buttons (`.big-btn`, `.secondary-btn`) to fit mobile screens better (padding and font size adjustment).
- **Margins**: Reduce margins for titles and containers to utilize screen space efficiently.

### 4. Verification
- Verify that the new styles are applied only on screens narrower than 768px.
- Ensure text is legible and not too small (minimum 12px) or too large.

### Example CSS Snippet
```css
@media (max-width: 768px) {
    /* Optimization for Blessing Words & Body Text */
    .scroll-text, p, .tutorial-hint, #banner-text {
        font-size: 1rem; /* 16px */
        line-height: 1.5;
        -webkit-font-smoothing: antialiased;
        text-shadow: 0 1px 2px rgba(0,0,0,0.8); /* Sharp shadow for contrast */
    }
    
    /* ... other adjustments ... */
}
```